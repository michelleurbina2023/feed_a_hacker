<a class="btn btn-danger" href="<%= home_path %>">Back to Home</a><br><br>

<table>
  <thead></thead>
  <tbody>
    <% @hackers.each_slice(5).each do |hackers| %>
     <tr>
      <% hackers.each do |hacker| %>
        <td><%= link_to image_tag(hacker.avatar_url, :size => "250x250"), hacker %></td>
      <% end %>
     </tr>
    <% end %>
  </tbody>
</table>
<br><br><br><br><br>